# VisionMaster Rules

## Core Purpose

VisionMaster reduces repetitive review work for welding vision production data. It is designed around real production CSVs and images generated by mass production battery vision instruments.

VisionMaster must never modify production CSVs, production images, or production PC folders. All copied, reviewed, staged, summarized, and exported data stays under `E:\KWAK\VisionMaster` by default.

## Production Safety Rules

Treat production PCs as read-only sources. Never delete, move, rename, or overwrite anything on a production PC.

Open CSVs and images with shared read/write/delete access because inspection files may be actively written while VisionMaster is reading them. For current-day or live CSVs, use local snapshots or safe read streams instead of locking the source file.

Missing files, inaccessible shares, actively writing files, incomplete images, and unavailable folders must be logged and handled without crashing the review queue. The user should be able to continue reviewing other cells.

## Machine Context

VisionMaster currently focuses on Welding `(+/-)` machines. Line/polarity output folders are `1-1(-)` through `2-2(+)`.

Default model rule: `1-1`, `1-2`, and `2-1` use `E81C`; `2-2` uses `E69B`. Welding image drives default to `E/F/G`; production data is on `D`.

The default storage root is `E:\KWAK\VisionMaster`.

## KickoutMonitor Workflow

The user selects one or more line/polarity machines and a date range. VisionMaster reads production daily CSVs from the selected welding PCs.

Only `JUDGE = NG` rows are reviewed. Cell IDs starting with `OCR` or `AGING` are ignored.

`UPPER_JUDGE` and `LOWER_JUDGE` decide which side images to display. Upper side displays `0_0`, `0_1`, `0_2`; lower side displays `1_0`, `1_1`, `1_2`. If both sides are NG, all 12 raw/overlay files are relevant.

The user classifies each queue item as Real NG, Overkill, or Multi-Defect NG. Real NG images stay grouped by defect. Overkill goes under `OVERKILL\<defect>`. Multi-defect NG is summarized as `MULTI-NG`.

## Kickout Summary Rules

A report date means `06:00:00` on the selected date through before `06:00:00` on the next day.

The summary cannot generate unless all NG rows in the requested window are reviewed. Overkill rate means `overkill / total inspected`. NG rates are line/polarity specific.

## IRS Review First Stage

IRS Review loads one IRS `.xlsx`. Include rows where `Request for NG Cell OUT == Request`. Ignore first judgment fields. Display `2nd judgment / reason`.

Use `Eqpt`, `Vision Type`, `Prod. date`, `Cell ID`, `Camera Location`, and `Image` to map the IRS row to production images. `PACKAGE #1-1 + Welding Plus` maps to `1-1(+)`; `PACKAGE #1-1 + Welding Minus` maps to `1-1(-)`.

First stage shows raw images only. The user chooses one or more manual categories: Rulebase, Undetectable, A L/R, B L/R, Micro LL/LM/MM/MR/RR, Tabside L/R, GAP, SEPA, and Sepa Shoulder L/R.

Rulebase and Undetectable are exclusive. Crop categories can be multi-selected. Enter commits the current selection and advances to the next IRS row.

## IRS First-Stage Save Rules

Rulebase saves the full original image folder to `IRS_LEAK\RULEBASE`. Undetectable saves the full original image folder to `IRS_LEAK\UNDETECTABLE`.

Crop selections save matching crop images to their Mavin folder-name destination: `Crop_A`, `Crop_B`, `Crop_micro`, `Crop_micro_tabside`, `Gap_DL`, `SEPA`, or `SEPA_SHOULDER`.

If selected crop files are missing, save the full original folder under `IRS_LEAK\NEED_TO_SIMULATE\<first-stage-class>\<original-folder-name>`.

Reclassifying a first-stage row deletes prior local saved results for that row and replaces them. This deletion applies only to VisionMaster local outputs, never production folders.

## IRS Crop Lookup Rules

Search bounded Mavin folder roots only: `\\<ip>\<drive>\Files\Image\<model>\<yyyy>\<MM>\<dd>\Mavin\<crop-folder>`.

Do not broad-scan production PCs. Search only the selected crop folders.

TOP means filenames containing upper-side evidence. BTM means filenames containing lower-side evidence. Useful crop files include `SourceMap`, `ActiveMap`, `SourceImg`, and `mask`. GAP may appear as `GAP_DL` or `Gap_DL`.

## IRS Final Stage / Dataset Workflow

`Generate Dataset` first checks that every IRS row was first-stage reviewed. The final-stage queue is based on saved first-stage results, not the original IRS workbook.

Normal crop items are grouped as source/overlay pairs by crop token. For example, `A_L SourceMap + A_L ActiveMap` is one final-stage item.

NEED_TO_SIMULATE final-stage items display only the 3 raw images from the defect side. NEED_TO_SIMULATE does not display overlays during final review.

Final-stage hotkeys are `R` for Real, `N` for No Need to Retrain, and `1-6` for the matching numbered subclass. Revisiting a classified final-stage item must show the previous final-class selection.

## IRS Summary / Dataset Export Rules

The summary cannot generate unless every final-stage item is classified. Normal crop final-stage items export to `Dataset\<crop-folder>\<final-class>\`.

NEED_TO_SIMULATE final-stage items export the whole original folder to `Dataset\NEED_TO_SIMULATE\<first-stage-class>\<original-folder-name>\`. NEED_TO_SIMULATE must not export as `Dataset\<first-stage-class>\Real`.

`No Need to Retrain` items are excluded from retrain dataset counts.

## Settings

Settings are stored locally in `E:\KWAK\VisionMaster\settings.json` by default. If the file is missing, VisionMaster creates defaults that match the current hard-coded behavior. If the file is invalid, VisionMaster loads defaults and shows a warning in the Settings page.

Editable settings include storage root, machine line/polarity/IP/model/drives/enabled flags, production path segments, Kickout ignored prefixes and report start time, IRS first-stage categories, and IRS final-stage class lists.

Low-level constants such as stable-file wait timing, copy throttling, zoom levels, image decode width, and 3 raw images per side remain code constants for now.
