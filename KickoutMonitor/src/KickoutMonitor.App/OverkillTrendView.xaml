<UserControl x:Class="KickoutMonitor.App.OverkillTrendView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:KickoutMonitor.App">
 <Grid Margin="10,6">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel>
   <TextBlock Text="{Binding Caption}" DockPanel.Dock="Right" Foreground="#64748B" Margin="10,3"/>
   <ItemsControl ItemsSource="{Binding Machines}">
    <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate><DataTemplate>
     <CheckBox IsChecked="{Binding Visible}" Foreground="{Binding Color}" Content="{Binding Line}" FontWeight="SemiBold" Margin="3,3,14,6"/>
    </DataTemplate></ItemsControl.ItemTemplate>
   </ItemsControl>
  </DockPanel>
  <local:OverkillLineChart x:Name="Chart" Grid.Row="1" Series="{Binding Series}"/>
  <DockPanel Grid.Row="2" Margin="0,8,0,4">
   <TextBlock Text="기간별 과검 건수" FontWeight="SemiBold" Margin="0,0,16,0"/>
   <Rectangle Width="100" Height="12" DockPanel.Dock="Right" Margin="8,0">
    <Rectangle.Fill><LinearGradientBrush StartPoint="0,0" EndPoint="1,0"><GradientStop Color="#EFF6FF" Offset="0"/><GradientStop Color="#2563EB" Offset="1"/></LinearGradientBrush></Rectangle.Fill>
   </Rectangle>
   <TextBlock Text="{Binding ScaleLabel}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
   <TextBlock Text="회색 — 자료 없음" Foreground="#64748B" Margin="12,0"/>
  </DockPanel>
  <ScrollViewer x:Name="HeatScroll" Grid.Row="3" MaxHeight="286" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch">
   <StackPanel>
    <DockPanel>
     <TextBlock Text="기계 / 극성" Width="90" VerticalAlignment="Center"/>
     <ItemsControl ItemsSource="{Binding Headers}">
      <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate><DataTemplate>
       <Button Content="{Binding Name}" Tag="{Binding Name}" Click="Field_Click" Width="120" Height="30" Margin="1" ToolTip="{Binding Name}" Padding="2">
        <Button.Style><Style TargetType="Button"><Setter Property="Background" Value="#F1F5F9"/><Style.Triggers>
         <DataTrigger Binding="{Binding Selected}" Value="True"><Setter Property="Background" Value="#DBEAFE"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="BorderBrush" Value="#2563EB"/></DataTrigger>
        </Style.Triggers></Style></Button.Style>
       </Button>
      </DataTemplate></ItemsControl.ItemTemplate>
     </ItemsControl>
    </DockPanel>
    <ItemsControl ItemsSource="{Binding HeatRows}">
     <ItemsControl.ItemTemplate><DataTemplate><DockPanel>
      <TextBlock Text="{Binding Line}" Width="90" VerticalAlignment="Center"/>
      <ItemsControl ItemsSource="{Binding Cells}">
       <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
       <ItemsControl.ItemTemplate><DataTemplate>
        <Border Background="{Binding Color}" Width="120" Height="27" Margin="1" ToolTip="{Binding Tooltip}">
         <Border.Style><Style TargetType="Border"><Setter Property="BorderThickness" Value="1"/><Setter Property="BorderBrush" Value="Transparent"/><Style.Triggers>
          <DataTrigger Binding="{Binding Selected}" Value="True"><Setter Property="BorderBrush" Value="#2563EB"/><Setter Property="BorderThickness" Value="2"/></DataTrigger>
         </Style.Triggers></Style></Border.Style>
         <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="25"/></Grid.ColumnDefinitions>
          <Button MinHeight="0" Margin="0" Background="Transparent" BorderThickness="0" Content="{Binding Text}" Tag="{Binding Field}" Click="Field_Click" Foreground="{Binding Foreground}" Padding="0"/>
          <Button Grid.Column="1" MinHeight="0" Margin="0" Background="Transparent" BorderThickness="0" Content="⋯" Click="Details_Click" ToolTip="해당 기계 · 항목 상세보기" Foreground="{Binding Foreground}" Padding="0"/>
         </Grid>
        </Border>
       </DataTemplate></ItemsControl.ItemTemplate>
      </ItemsControl>
     </DockPanel></DataTemplate></ItemsControl.ItemTemplate>
    </ItemsControl>
   </StackPanel>
  </ScrollViewer>
 </Grid>
</UserControl>
