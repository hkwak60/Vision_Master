<UserControl Background="#F3F5F7" Foreground="#172B4D" FontFamily="Segoe UI" x:Class="KickoutMonitor.App.OverkillMonitorView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:KickoutMonitor.App">
 <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolVisibility"/></UserControl.Resources>
 <DockPanel Margin="14">
  <StackPanel DockPanel.Dock="Top">
   <DockPanel><Button Content="Back" Click="Back_Click" DockPanel.Dock="Left" Margin="0,0,14,0"/><TextBlock Text="Overkill Monitor" FontSize="24" FontWeight="SemiBold"/></DockPanel>
   <WrapPanel Margin="0,10">
    <TextBlock Text="시작일" VerticalAlignment="Center"/><DatePicker SelectedDate="{Binding StartDate}" Width="130" Margin="6,0,12,0"/>
    <TextBlock Text="종료일" VerticalAlignment="Center"/><DatePicker SelectedDate="{Binding EndDate}" Width="130" Margin="6,0,12,0"/>
    <Button Content="최근 7일" Command="{Binding RecentSevenCommand}" Padding="10,4" Margin="0,0,16,0"/>
    <StackPanel Orientation="Horizontal" Visibility="{Binding IsTrendTab, Converter={StaticResource BoolVisibility}}">
     <TextBlock Text="항목" VerticalAlignment="Center"/><ComboBox ItemsSource="{Binding Fields}" SelectedItem="{Binding Field}" Width="160" Margin="6,0,12,0"/>
    </StackPanel>
    <Button Content="새로고침" Command="{Binding RefreshCommand}" Padding="14,4"/>
   </WrapPanel>
  </StackPanel>
  <TextBlock DockPanel.Dock="Bottom" Text="{Binding Status}" TextWrapping="Wrap" Foreground="#B45309"/>
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <TabControl x:Name="Tabs" SelectedIndex="{Binding ActiveTab}">
    <TabItem Header="Kickout"><local:OverkillTrendView DataContext="{Binding Kickout}"/></TabItem>
    <TabItem Header="DLNG"><local:OverkillTrendView DataContext="{Binding Dlng}"/></TabItem>
   <TabItem Header="Training collection">
    <Grid Margin="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="*"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <StackPanel><TextBlock Text="One pair = one sample. Trained batches retain their counts and can be exported again. Select a batch to see its class counts." TextWrapping="Wrap"/>
      <WrapPanel Margin="0,8"><Button Content="Retry pending copies" Command="{Binding RetryCommand}"/><Button Content="Generate Dataset" Command="{Binding GenerateDatasetCommand}" Margin="10,0"/><Button Content="Exclude selected sample" Command="{Binding ExcludeCommand}"/></WrapPanel>
     </StackPanel>
     <DataGrid Grid.Row="1" ItemsSource="{Binding Batches}" SelectedItem="{Binding SelectedBatch}" IsReadOnly="True" AutoGenerateColumns="False">
      <DataGrid.Columns><DataGridTextColumn Header="Crop" Binding="{Binding Crop}"/><DataGridTextColumn Header="Polarity" Binding="{Binding Polarity}"/>
       <DataGridTextColumn Header="Collection dates" Binding="{Binding Range}"/><DataGridTextColumn Header="Samples" Binding="{Binding TotalSamples}"/><DataGridTextColumn Header="Image files" Binding="{Binding TotalFiles}"/><DataGridTextColumn Header="Status" Binding="{Binding Status}"/><DataGridTextColumn Header="Pending/failed" Binding="{Binding Pending}"/><DataGridTextColumn Header="Trained" Binding="{Binding TrainedAt}"/><DataGridTextColumn Header="Dataset folder" Binding="{Binding DatasetFolder}" Width="220"/><DataGridTextColumn Header="Batch ID" Binding="{Binding Id}"/></DataGrid.Columns>
     </DataGrid>
     <DataGrid Grid.Row="2" ItemsSource="{Binding Samples}" SelectedItem="{Binding SelectedSample}" IsReadOnly="True" AutoGenerateColumns="False" Margin="0,8">
      <DataGrid.Columns><DataGridTextColumn Header="Cell" Binding="{Binding Review.CellId}"/><DataGridTextColumn Header="Line" Binding="{Binding Review.LinePolarity}"/><DataGridTextColumn Header="Final class" Binding="{Binding Review.FinalClass}"/><DataGridTextColumn Header="Copy state" Binding="{Binding State}"/><DataGridTextColumn Header="Collected" Binding="{Binding CollectedAt}"/><DataGridCheckBoxColumn Header="Superseded" Binding="{Binding Superseded}"/><DataGridTextColumn Header="Details" Binding="{Binding Error}" Width="*"/></DataGrid.Columns>
     </DataGrid>
     <DataGrid Grid.Row="3" ItemsSource="{Binding CollectionCounts}" IsReadOnly="True" AutoGenerateColumns="False"><DataGrid.Columns>
       <DataGridTextColumn Header="Crop" Binding="{Binding Crop}"/><DataGridTextColumn Header="Line" Binding="{Binding Line}"/>
       <DataGridTextColumn Header="Final class" Binding="{Binding FinalClass}"/><DataGridTextColumn Header="Samples" Binding="{Binding Samples}"/><DataGridTextColumn Header="Files" Binding="{Binding Files}"/>
      </DataGrid.Columns></DataGrid>
    </Grid>
   </TabItem>

   </TabControl>
   <Border Grid.Row="1" Visibility="{Binding DetailsOpen, Converter={StaticResource BoolVisibility}}" BorderBrush="#CBD5E1" BorderThickness="1" Background="White" Margin="0,6,0,0">
    <DockPanel Margin="8">
     <DockPanel DockPanel.Dock="Top" Margin="0,0,0,5">
      <Button Content="닫기 ×" Command="{Binding CloseDetailsCommand}" DockPanel.Dock="Right" Padding="10,3"/>
      <TextBlock Text="{Binding DetailTitle}" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
     </DockPanel>
     <DataGrid ItemsSource="{Binding Contributions}" Height="130" AutoGenerateColumns="False" IsReadOnly="True" HorizontalScrollBarVisibility="Auto">
      <DataGrid.Columns>
       <DataGridTextColumn Header="생산일" Binding="{Binding Day}"/><DataGridTextColumn Header="기계" Binding="{Binding Line}"/>
       <DataGridTextColumn Header="항목" Binding="{Binding Field}"/><DataGridTextColumn Header="과검" Binding="{Binding Overkill}"/>
       <DataGridTextColumn Header="원판정" Binding="{Binding SourceClass}"/><DataGridTextColumn Header="최종판정" Binding="{Binding FinalClass}"/>
       <DataGridTextColumn Header="리뷰 수" Binding="{Binding Reviewed}"/><DataGridTextColumn Header="검사 수" Binding="{Binding Inspected}"/>
       <DataGridTextColumn Header="기록" Binding="{Binding Identity}" Width="280"/><DataGridTextColumn Header="출처" Binding="{Binding Source}" Width="300"/>
      </DataGrid.Columns>
     </DataGrid>
    </DockPanel>
   </Border>
  </Grid>
 </DockPanel>
</UserControl>
